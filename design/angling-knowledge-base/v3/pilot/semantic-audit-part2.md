## 5. Regulation audit (all 5 provisions)

| Provision | Issuing authority | Exact source | Effective interval | Verified | Geographic match | Atomic fact | Official wording | Composes with |
|---|---|---|---|---|---|---|---|---|
| statewide daily_limit | Minnesota DNR | [DNR news release, Mar 5 2026](https://www.dnr.state.mn.us/news/2026/03/05/minnesota-dnr-keeps-three-walleye-limit-2026-open-water-season-mille-lacs-lake) | 2026-05-09 – open | 2026-08-28 | statewide (no waterbody_id — correctly draft-only per the schema's own rule that published named_water needs a real FK; statewide scope never required one) | daily_limit = 6, combined with sauger | "Statewide inland default: 6 walleye+sauger combined, only one over 20 inches." | size_rule (statewide), catch_and_release_permitted |
| statewide size_rule | Minnesota DNR | same release | 2026-05-09 – open | 2026-08-28 | statewide | one_over_threshold, 20in | "only one walleye over 20 inches in possession" | statewide daily_limit |
| Mille Lacs daily_limit | Minnesota DNR | same release | 2026-05-09 – open | 2026-08-28 | **named_water, waterbody_id is a placeholder UUID (`dddddddd-...`), not a real FK resolved against the actual `waterbodies` table** — flagged, not hidden | daily_limit = 3 | "harvest walleye 17 inches or greater... only one over 20 inches" | Mille Lacs size_rule; SUPERSEDES the statewide daily_limit for this water by scope precedence |
| Mille Lacs size_rule | Minnesota DNR | same release | 2026-05-09 – open | 2026-08-28 | same placeholder waterbody_id issue | minimum, 17in | "17 inches or greater" | Mille Lacs daily_limit |
| statewide catch_and_release_permitted | Minnesota DNR (inferred from the general regulatory framework documented in Section 1 of the readable artifact, not this specific release) | Section-1 research, not this specific news release — **this is the weakest-sourced provision of the five, its `evidence` claim currently over-cites the Mille Lacs release, which does not actually state a general catch-and-release permission** | 2026-05-09 – open (borrowed from the other provisions, not independently verified) | 2026-08-28 | statewide | boolean true | (paraphrase, not a verbatim quote from the cited release — a real defect) | all others |

**Real finding, not hidden**: the `catch_and_release_permitted` provision's `evidence` cites the Mille Lacs walleye-limit release, but that release does not actually state a general catch-and-release permission — this is a genuine **unsupported claim**, exactly the kind of thing instruction 3 asks to be caught. **Correction**: either find a real DNR source stating this explicitly, or downgrade this provision's own confidence/mark it unverified pending a real citation. **No non-official source establishes legality anywhere in this pilot** — confirmed true; all 5 provisions cite MN DNR, never a secondary aggregator — but one of the five cites DNR for a claim that specific page doesn't make, which is a citation-accuracy defect, not a source-type defect.

All 5 named_water provisions use a **placeholder waterbody_id**, not one resolved against the real `waterbodies` table — correctly blocked from `published` status by the schema's own CHECK constraint (verified in gate 3's DB tests), but worth restating here as a real, unresolved prerequisite before any of these could ever be approved for production, not just a pilot detail.

## 6. Scenario review (condensed — full raw output in `scenarios-output.txt`)

Given the volume (15 scenarios × full mission input × every candidate), this section reports the substantive findings rather than re-printing the already-committed raw output:

- **Scenarios 6, 7, 9 (direct hits on t1/t2/t4/t5's own condition windows)**: top result is practically reasonable and matches the tactic it was built to match — expected, low information value as a test (I wrote the scenario to match the tactic).
- **Scenario 1 (post-front, shore, no live bait)**: only 1 survivor, score 0.000. **Practically unreasonable as a production result** — a real angler would get a recommendation with a 0.000 confidence-adjusted match and no explanation of why. **Missing tactic records, identified but not generated**: (a) a shore/wading tactic that actually constrains `weather_front`/`barometric_pressure_trend`, (b) any artificial-only tactic for cold/post-front conditions specifically (t11 is the only shore+artificial tactic and it's built for post-spawn/stained/low-light, not post-front).
- **Scenario 7 (the tied conflict)**: confirmed both tactics present at identical scores (0.402/0.402) after the ORIGINAL (pre-recalculation) confidence weights. **After the confidence recalculation in section 3, both t4 and t5 drop to `estimated` (weight 0.4 vs. their old `anecdotal` weight 0.55)** — the tie itself is preserved (they still match identically on conditions, and now share the same recalculated confidence too), so **FishWizz would still present both with the disagreement explanation, not resolve the tie arbitrarily** — confirmed by construction, not just by policy: nothing in the scorer breaks ties by anything other than condition-match, so two identical-condition, identical-confidence tactics remain genuinely tied.
- **Scenario 12 (artificial_only)**: t8 survived the filter because it carries BOTH `live_bait` and `artificial_only` tags (a bare jig can be fished either way) — **this is a real, undesirable ambiguity**: a hard hard-bait-only user should arguably not be shown a tactic whose `rigging_instructions` text still says "tipped with a minnow." Flagged as a real tag-modeling gap, not a scorer bug.
- **General pattern across all 15**: excluded-axis reasons are dominated by `mission_missing` (most scenarios only specify 3-6 of 19 axes) and `no_constraint` (most tactics only specify 6-9 of 19 axes) — the pilot's own condition specificity is real but partial, consistent with 15 hand/generator-authored tactics rather than a mature dataset.

## 7. Manual fishing-logic review (real findings, not a clean bill of health)

- **t8 (kayak finesse) — lure weight vs. line class**: 4-6lb line with a 1/16-1/8oz jig is internally consistent (light line, light jig) — **passes**.
- **t2 vs. t14 equipment scaling — passes**: t14 (strong river current) correctly specifies heavier line (12-17lb) and heavier weight (1-2oz) than t2's stillwater version (6-10lb, 0.5-1oz) — the current-driven equipment scaling is directionally correct fishing logic.
- **t7/t13 (dock) casting distance — not actually checked**: `casting_access_required: 'limited'` is set, but no tactic states an actual casting distance, so "shore casting distance is realistic" **cannot be verified either way from the current schema** — a real gap: the schema has no distance field to audit against.
- **t8/t11 kayak/canoe safety — real gap**: t8's `fails_when` correctly flags wind/control as the failure mode, but **no tactic anywhere states a wind-speed threshold** at which kayak/canoe becomes unsafe — the schema's `wind` enum (`calm/light/moderate/high`) is a condition axis, not a safety threshold, and nothing enforces "exclude kayak/canoe tactics when wind=high" at the scorer level today. This is a real, not-yet-closed safety gap, distinct from and more concrete than the general "safety constraints such as wind limits by platform" item already listed as a schema gap.
- **Seasonal stage vs. temperature — passes for all 15**: spot-checked every tactic's `biological_stage` against its `water_temp_f` range (e.g., t4/t5 `midwinter_ice` at 34-39°F, t1 `pre_spawn` at 42-55°F, t3 `fall_turnover` at 48-62°F) — internally consistent with standard MN/WI seasonal water-temperature bands.
- **works_when/fails_when vs. applies_when — passes for all 15**: no tactic's prose contradicts its own structured condition envelope (spot-checked by re-reading all 15 pairs).
- **next_try usable under failure conditions — mostly passes, one real gap**: t9 (turbid crawler harness) `fails_when` says "clear water... a subtler presentation typically outperforms," but t9 has **no `alternatives` entry pointing to a clear-water tactic at all** — a real, actionable next_try gap, not yet fixed.
- **Conservation guidance — thin, not wrong**: only t2 and t15 carry `conservation_notes` (barotrauma for deep summer fish; handling-time for stressed late-ice fish) — both are species/depth/temperature-appropriate where present, but 13 of 15 tactics have `null` conservation_notes, including t10 (deep sauger, plausibly also barotrauma-relevant) — a real coverage gap, not incorrect content.

## 8. Remaining evidence gaps (explicit list)

1. No sauger-specific primary source found this session (t6, t10, t12, t14 all rest on the non-independent placeholder).
2. No independent second source for ANY walleye claim — every real citation traces to one of two MN DNR pages; nothing reaches true `established`.
3. Equipment numeric ranges (line test, lure weight, pause duration) are self-authored "standard tackle" figures on all 15 tactics, not sourced from any fetched material — precision was wrongly marked `exact`.
4. `catch_and_release_permitted` provision's citation does not actually support its claim (section 5).
5. All `named_water` regulation `waterbody_id`s are placeholders, not resolved against the real `waterbodies` table.
6. Scenario 1's condition combination (post-front, shore, no live bait) has no well-matched tactic in the 15-tactic set.
7. t9 has no usable `next_try` for its own stated failure condition.
8. No kayak/canoe wind-speed safety threshold exists anywhere in the schema or data.
9. Tags like t8's simultaneous `live_bait`+`artificial_only` can let a bait-based tactic survive an artificial-only filter on a technicality.
10. 13 of 15 tactics have no conservation guidance at all (not wrong, just absent).
